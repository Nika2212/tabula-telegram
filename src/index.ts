/**
 * Entry point: load config, build dependencies, start the pipeline.
 * Wiring lives here; every collaborator is constructed and injected.
 */
async function main(): Promise<void> {
  throw new Error('bootstrap is not implemented');
}

await main();
